<UserControl
    x:Name="pageRoot"
    x:Class="Microsoft.Samples.Kinect.ControlsBasics.KinectPointerPointSample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ControlsBasics_XAML"
    xmlns:common="using:ControlsBasics_XAML.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="25" />
            <Setter Property="Foreground" Value="White" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid Grid.Row="1">
            <Canvas Name="mainScreen" Background="Transparent" Grid.Column="0" Grid.Row="0" Margin="1"  />
        </Grid>
    </Grid>
</UserControl>
